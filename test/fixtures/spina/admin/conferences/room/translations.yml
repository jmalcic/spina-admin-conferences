# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
room_without_dependencies:
  building: Room without dependencies
  number: 1
  locale: en-GB
  translated_model: room_without_dependencies

room_with_sessions:
  building: Room with sessions
  number: 2
  locale: en-GB
  translated_model: room_with_sessions

room_with_presentations:
  building: Room with presentations
  number: foyer
  locale: en-GB
  translated_model: room_with_presentations
