.flex-auto.d-flex.flex-column{ style: 'z-index: 0' }
  - cache [latest_conference, @page] do
    = render content(:gallery)&.images&.any? ? content(:gallery) : 'shared/jumbotron', advance: true do
      .py-3.py-lg-6
        .text-white
          %p.h000-mktg.text-shadow-dark.lh-condensed-ultra= latest_conference.name
          %address.f2-light.text-shadow-dark= latest_conference.institution.name
          %time.f2-light.text-shadow-dark<
            = time_tag latest_conference.dates.min, latest_conference.dates.min.strftime('%d/%m')
            –
            = time_tag latest_conference.dates.max, latest_conference.dates.max.strftime('%d/%m')
          .markdown-body.my-4.text-shadow-dark= content(:text).try(:html_safe)
          .d-block.mt-4= link_to 'More info', spina.conferences_conference_path(latest_conference), class: %w(btn btn-large f3)
  %footer.px-3.px-md-4
    .container-md= render 'shared/footer'
