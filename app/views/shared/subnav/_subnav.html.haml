.subnav
  = yield
  %nav.subnav-links{ aria: { label: '' } }
    .UnderlineNav-body
      - collection.each do |item|
        %button.btn-link.subnav-item{ title: item.send(method), target: '_self', role: 'tab', data: { itemnav: { target: 'item' }, action: 'itemnav#select' } }= item.send(method)
