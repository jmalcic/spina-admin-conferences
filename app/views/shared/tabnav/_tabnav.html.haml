.tabnav
  = yield
  %nav.tabnav-tabs{ aria: { label: '' } }
    .UnderlineNav-body
      - collection.each do |item|
        %button.tabnav-tab{ title: item.send(method), target: '_self', role: 'tab', data: { itemnav: { target: 'item' }, action: 'itemnav#select' } }= item.send(method)
