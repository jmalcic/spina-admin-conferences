- content_for :application do
  %header#header
    #header_actions
      = yield(:header_actions)

    = render partial: 'spina/admin/shared/breadcrumbs'

    %nav#secondary.tabs
      %ul
        %li{class: ('active' if controller_name == 'conferences')}
          = link_to t('spina.collect.website.conferences'), admin_collect_conferences_path
        %li{class: ('active' if controller_name == 'presentation_types')}
          = link_to t('spina.collect.website.presentation_types'), admin_collect_presentation_types_path

  = yield

= render template: 'layouts/spina/admin/admin'
