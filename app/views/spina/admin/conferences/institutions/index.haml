- content_for(:header_actions) do
  = link_to spina.new_admin_conferences_institution_path, class: 'button button-primary', style: 'margin-right: 0' do
    = icon('plus')
    = t('spina.conferences.institutions.new')

.well
  .table-container
    %table.table
      %thead
        %tr
          %th= Spina::Conferences::Institution.human_attribute_name :name
          %th= Spina::Conferences::Institution.human_attribute_name :city
          %th

      %tbody
        - if @institutions.any?
          - @institutions.each do |institution|
            %tr
              %td= link_to institution.name, spina.edit_admin_conferences_institution_path(institution)
              %td= institution.city
              %td.align-right
                = link_to spina.edit_admin_conferences_institution_path(institution), class: 'button button-small button-link' do
                  = icon('pencil-outline')
                  = t('spina.edit')

        - else
          %tr
            %td.align-center{ colspan: 100 }
              %em= t('spina.conferences.institutions.no_institutions')
