#delegates.tab-content.well
  .table-container
    %table.table
      %thead
        %tr
          %th= Spina::Conferences::Delegate.human_attribute_name :name
          %th= Spina::Conferences::Delegate.human_attribute_name :email_address
          %th

      %tbody
        - if @dietary_requirement.delegates.any?
          = render partial: 'spina/admin/conferences/shared/delegate', collection: @dietary_requirement.delegates

        - else
          %tr
            %td.align-center{ colspan: 100 }
              %em= t('spina.conferences.delegates.no_delegates')