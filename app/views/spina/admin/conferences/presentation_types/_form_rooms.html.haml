#rooms.tab-content.well
  .table-container
    %table.table
      %thead
        %tr
          %th= Spina::Conferences::Room.human_attribute_name :building
          %th= Spina::Conferences::Room.human_attribute_name :number
          %th

      %tbody
        - if @presentation_type.room_possessions.any?
          = render partial: 'spina/admin/conferences/shared/room', collection: @presentation_type.room_possessions.collect(&:room)

        - else
          %tr
            %td.align-center{ colspan: 100 }
              %em= t('spina.conferences.rooms.no_rooms')
